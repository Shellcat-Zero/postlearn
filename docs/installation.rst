.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install postlearn

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv postlearn
    $ pip install postlearn
